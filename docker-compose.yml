version: '3'
services:

  db:
    image: postgres
    restart: always
    container_name: "my_postgres"
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pwd0123456789
